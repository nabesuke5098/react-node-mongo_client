import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    marginLeft: 'auto',
    marginRight: 'auto',
    verticalAlign: 'middle', 
    maxWidth: '90%',
    minHeight: 'auto',
    margin: '25px 0',
    overflow: 'hidden',
    width: '100%'
  },
  media: {
    height: 280,
  },
});
import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    marginLeft: 'auto',
    marginRight: 'auto',
    verticalAlign: 'middle', 
    maxWidth: '90%',
    minHeight: 600,
  },
  media: {
    height: 140,
  },
});